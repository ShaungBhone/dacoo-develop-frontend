import { InboxView } from "@/components/inbox/inbox-view"

export const metadata = {
  title: "Inbox",
  description: "Manage inbound conversations from every connected channel",
}

export default function InboxPage() {
  return <InboxView />
}
